const DataView = ({ ...props }) => {
  return (
    <>
      {/* {JSON.stringify(myCoordinates)} */}



      {/* <p>
              Device compass permission:
            </p>
            <p>
              {devicePermission.message}
            </p>
            {

              <button onClick={() => requestDevicePermission(window)}>Request permission</button>
            }
            <ul>
              <li>Alpha: {orientationData.alpha && orientationData.alpha.toFixed()}</li>
              <li>Beta: {orientationData.beta && orientationData.beta.toFixed()}</li>
              <li>Gamma: {orientationData.gamma && orientationData.gamma.toFixed()}</li>
              <hr />
              <li>webkitCompassAccuracy: {orientationData.webkitCompassAccuracy && `${getCompassAccuracyPercentage(orientationData.webkitCompassAccuracy)}% (${orientationData.webkitCompassAccuracy.toFixed(4)})`}</li>
              <li>webkitCompassHeading: {orientationData.webkitCompassHeading && orientationData.webkitCompassHeading.toFixed()}</li>
            </ul> */}</>
  )
}
export default DataView